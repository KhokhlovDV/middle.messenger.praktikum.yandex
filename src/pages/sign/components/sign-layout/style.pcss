.sign-layout {
    display: flex;
    flex-direction: column;
    padding: 50px 30px 30px;
    height: 100%;
}

.sign-layout__header {
    margin-bottom: 40px;
}

.sign-layout__form {
    display: flex;
    flex: 1;
    flex-direction: column;
    justify-content: space-between;
}

.sign-layout__button {
    margin-bottom: 14px;
}

.sign-layout__link {
    display: block;
    margin: 0 auto;
    width: fit-content;
}
