.actions {
    height: 100vh;
    left: 0;
    position: fixed;
    top: 0;
    width: 100vw;
    z-index: 1000;
}

.actions__container {
    background-color: var(--on-accent-color);
    border-radius: 12px;
    box-shadow: 0 0 6px 0 #00000024;
    display: flex;
    flex-direction: column;
    gap: 16px;
    padding: 15px;
    position: absolute;
    right: 10px;
    top: 68px;
}
