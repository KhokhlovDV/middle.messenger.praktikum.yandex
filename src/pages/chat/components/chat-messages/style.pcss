.chat-messages {
    align-items: start;
    border-color: var(--primary-color);
    border-style: solid;
    border-width: 1px 0;
    display: flex;
    flex: 1;
    flex-direction: column;
    gap: 12px;
    overflow: auto;
    padding-bottom: 20px;
    padding-right: 8px;
    padding-top: 20px;
}
