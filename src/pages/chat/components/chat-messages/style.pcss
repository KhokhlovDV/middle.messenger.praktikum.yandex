.chat-messages {
    flex: 1;
    border-color: var(--primary-color);
    border-style: solid;
    border-width: 1px 0;
    padding-top: 20px;
    overflow: auto;
    display: flex;
    flex-direction: column;
}
