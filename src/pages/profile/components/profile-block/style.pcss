.profile-block {
    display: flex;
    flex-direction: column;
    gap: 10px;
    width: 510px;

    & > * {
        &:not(:last-child) {
            border-bottom: 1px solid var(--primary-color);
            padding-bottom: 10px;
        }
    }
}
